# Save exact versions
save-exact=true

# Use lockfile
package-lock=true

# Set registry (optional - uncomment if needed)
# registry=https://registry.npmjs.org/

# Prevent auto-install of peer dependencies
legacy-peer-deps=false

# Engine strict mode (enforce Node version)
engine-strict=true
